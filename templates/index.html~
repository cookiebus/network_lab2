{% extends "base.html" %}
{% load staticfiles %}
{% block title %}{{person.name}}{% endblock %}

{% block content %}
    
    {{ person.name }}<a href="{{person.url}}">{{person.url}}</a>
  
{% endblock %}
